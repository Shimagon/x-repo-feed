{
  "name": "x-repo-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polls X (@mijukumono_AI) for reposts containing GitHub URLs and forwards them to Gmail with [repo-feed] subject.",
  "scripts": {
    "poll": "node scripts/poll.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "playwright": "^1.50.0"
  },
  "engines": {
    "node": ">=20"
  }
}
